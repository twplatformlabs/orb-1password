# /src/@orb.yml
# yamllint disable rule:line-length
---
version: 2.1

description: |
  Orb for using 1password secrets automation within your pipeline.

  Available commands include:

  - install: install 1password CLI
  - env: set shell ENV variables from an environment file (e.g., op.env)
  - tpl: op inject template file to output file
  - write-to-file: write secret from op to named file


  NOTE: v3.x.x is a breaking change. See detailed documentation before upgrading.

display:
  source_url: "https://github.com/ThoughtWorks-DPS/orb-1password"
  home_url: "https://github.com/ThoughtWorks-DPS/orb-1password"
